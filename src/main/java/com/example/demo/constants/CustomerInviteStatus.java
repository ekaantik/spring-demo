package com.example.demo.constants;

public enum CustomerInviteStatus {
    INVITE_SENT,
    INVITE_ACCEPTED,
    INVITE_REJECTED // NOT IMPLEMENTED YET, if invite expires it will be marked as rejected
}
